# shared/api

`src/shared/api` 用于放置跨业务模块复用的 API 客户端、请求类型和响应类型。

放置规则：

- 只服务单个页面或单个业务域的接口，优先放在对应 feature 或页面附近。
- 被多个业务域复用的接口，放到本目录。
- 新增 API 时保持与 `src/api/http.ts` 的请求封装一致，避免直接散落 `fetch` 或第三方请求库调用。

